---
phase: 1
wave: 1
requirements: ["ENV-01", "DB-01", "DB-02", "DB-03"]
files_modified: ["convex/schema.ts", "convex/decisions.ts", "convex/state.ts"]
autonomous: true
---

# Plan 01-01: Convex Schema and Mutations Setup

## Objective
Initialize the Convex backend with the reactive schema and core mutations/queries required for the trading agent.

## Tasks

<task>
<read_first>
- [ROADMAP.md](file:///d:/Hackaton/ai-trading-agents/.planning/ROADMAP.md)
</read_first>
<action>
Create `convex/schema.ts` with labels `decisions` and `state`.
</action>
<acceptance_criteria>
`convex/schema.ts` exists and defines both tables with appropriate fields.
</acceptance_criteria>
</task>

<task>
<read_first>
- [convex/schema.ts](file:///d:/Hackaton/ai-trading-agents/convex/schema.ts)
</read_first>
<action>
Create `convex/decisions.ts` with `insertDecision` mutation and `getRecent` query.
</action>
<acceptance_criteria>
`convex/decisions.ts` contains `insertDecision` which takes all schema fields.
</acceptance_criteria>
</task>

<task>
<read_first>
- [convex/schema.ts](file:///d:/Hackaton/ai-trading-agents/convex/schema.ts)
</read_first>
<action>
Create `convex/state.ts` with `getValue` query and `upsertValue` mutation.
</action>
<acceptance_criteria>
`convex/state.ts` contains `upsertValue` which uses patch or insert.
</acceptance_criteria>
</task>

## Verification
- `npx convex dev --once` (to check for schema errors)
